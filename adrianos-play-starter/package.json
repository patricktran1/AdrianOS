{
  "name": "adrianos-play",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:games": "node scripts/generate-games.mjs",
    "dev": "npm run generate:games && next dev",
    "build": "npm run generate:games && npm run check:school-controls && next build",
    "start": "next start",
    "check:school-controls": "node scripts/check-school-mode-controls.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "next": "^16.2.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.9.0"
  }
}
